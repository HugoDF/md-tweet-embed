{
  "name": "md-tweet-embed",
  "version": "0.2.0",
  "description": "Convert Tweets to Markdown embeds",
  "main": "./md-tweet-embed.js",
  "type": "module",
  "bin": "./cli.js",
  "files": [
    "cli.js",
    "md-tweet-embed.js"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node tests",
    "fmt": "npx xo --prettier --space --global=fetch --fix",
    "release": "npx np"
  },
  "dependencies": {
    "turndown": "^7.0.0"
  },
  "license": "MIT",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  }
}
